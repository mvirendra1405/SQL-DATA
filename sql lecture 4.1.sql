select * from student;

-- this is use for fetch the details
select student.fname,student.contact from student;

select fname,iname,contact from student;

select fname as first_name ,iname as last_name,contact from student;s